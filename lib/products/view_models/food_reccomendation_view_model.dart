// ignore_for_file: depend_on_referenced_packages

import 'package:flutter/material.dart';
import 'dart:developer';

class FoodReccomendationViewModel extends ChangeNotifier {
  // late Interpreter model;
  // late InterpreterOptions interpreterOptions;
  //   late List<int> _inputShape;
  // late List<int> _outputShape;

  // late TensorImage _inputImage;
  // late TensorBuffer _outputBuffer;

  // late TfLiteType _inputType;
  // late TfLiteType _outputType;

  // Future loadAIModelFromAsset() async {
  //   interpreterOptions = InterpreterOptions();
  //   model = await Interpreter.fromAsset(
  //     'assets/ai/age_gender_estimation.tflite',
  //     options: interpreterOptions,
  //   );
  //   log("success");
  // }

  // FoodReccomendationViewModel() {
  //   loadAIModelFromAsset();
  // }

  // Future runModel(String imagePath, int age, bool gender) async {
  //   int genderVal = gender ? 1 : 0;
  //   var input = [
  //     [imagePath, age, genderVal]
  //   ];
  //   var output = List.filled(1 * 2, 0).reshape([2, 1]);
  //   model.run(input, output);
  //   notifyListeners();
  //   print(output);
  // }
}
